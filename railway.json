{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "php artisan optimize:clear && php artisan config:cache && php artisan migrate --force && php -S 0.0.0.0:${PORT:-8080} -t public"
  }
}
