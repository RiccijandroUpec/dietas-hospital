{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "php artisan config:cache && php artisan migrate --force && php artisan db:seed --class=RegistrationScheduleSeeder --force && php artisan db:seed --class=UpdateMedicinaInternaSeeder --force && php artisan view:cache && php -S 0.0.0.0:${PORT:-8080} -t public"
  }
}
